# packing

## Development

1. Make changes in list.cljc
2. `make serve` to serve locally and see changes.
3. `make test` to run the same Clojure test suite as CI.
4. `make build` to copy the static assets into `../site/Packing` (mirrors the GitHub Pages workflow).
